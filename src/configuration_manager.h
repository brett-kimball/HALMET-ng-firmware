// Compatibility header. New implementation lives in config_manager.h.
#pragma once

#include "config_manager.h"

// Nothing else here — this header preserves the old include path for
// existing code but reuses the types and functions declared in
// config_manager.h.
